# incoming/

Coloca aquí el .zip con el código local de la plataforma (Lanson/Blindsystem).

No se sube a git (ver `.gitignore`) — es solo el punto de entrega para que se escanee y, de ahí, se organice el código real dentro del repo.
